<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Dipendenze self-contained: token Base necessari per {StaticResource ...} -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/PlateArchive.Ui;component/Themes/Base/Spacing.xaml"/>
        <ResourceDictionary Source="/PlateArchive.Ui;component/Themes/Base/CornerRadius.xaml"/>
        <ResourceDictionary Source="/PlateArchive.Ui;component/Themes/Base/Typography.xaml"/>
        <ResourceDictionary Source="/PlateArchive.Ui;component/Themes/Base/Elevation.xaml"/>
    </ResourceDictionary.MergedDictionaries>


    <!-- BASE CARD -->
    <Style x:Key="Card.Base" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Brush.Surface}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{StaticResource Radius.L}" />
        <Setter Property="Padding" Value="{StaticResource Padding.Card}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <!-- SUBTLE CARD -->
    <Style x:Key="Card.Subtle"
           TargetType="Border"
           BasedOn="{StaticResource Card.Base}">
        <Setter Property="Background" Value="{DynamicResource Brush.SurfaceAlt}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}" />
    </Style>

    <!-- ELEVATED CARD -->
    <Style x:Key="Card.Elevated"
           TargetType="Border"
           BasedOn="{StaticResource Card.Base}">
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.BorderStrong}" />
    </Style>

    <!-- ACCENT CARD -->
    <Style x:Key="Card.Accent"
           TargetType="Border"
           BasedOn="{StaticResource Card.Base}">
        <Setter Property="Background" Value="{DynamicResource Brush.PrimaryLighter}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.PrimaryLight}" />
    </Style>

</ResourceDictionary>