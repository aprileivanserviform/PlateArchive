<UserControl x:Class="PlateArchive.Wpf.Controls.NumericBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="38">

    <UserControl.Resources>

        <!-- TextBox senza bordo né sfondo: il chrome è fornito dall'outer Border -->
        <Style x:Key="Inner.TextBox" TargetType="TextBox">
            <Setter Property="Background"             Value="Transparent"/>
            <Setter Property="BorderThickness"        Value="0"/>
            <Setter Property="Padding"                Value="10,0,4,0"/>
            <Setter Property="VerticalAlignment"      Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontFamily"             Value="{DynamicResource FontFamily.Base}"/>
            <Setter Property="FontSize"               Value="{DynamicResource FontSize.Body}"/>
            <Setter Property="Foreground"             Value="{DynamicResource Brush.InputForeground}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- RepeatButton compatto senza bordo -->
        <Style x:Key="SpinBtn" TargetType="RepeatButton">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="2"/>
            <Setter Property="Focusable"       Value="False"/>
            <Setter Property="Cursor"          Value="Arrow"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border x:Name="Root" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background"
                                        Value="{DynamicResource Brush.ItemHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background"
                                        Value="{DynamicResource Brush.ControlPressed}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <!-- Outer border: cambia colore al focus della TextBox interna tramite IsKeyboardFocusWithin -->
    <Border ClipToBounds="True"
            Background="{DynamicResource Brush.InputBackground}"
            BorderThickness="1"
            CornerRadius="{StaticResource Radius.M}">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.InputBorder}"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" Value="{DynamicResource Brush.BorderStrong}"/>
                    </Trigger>
                    <Trigger Property="IsKeyboardFocusWithin" Value="True">
                        <Setter Property="BorderBrush" Value="{DynamicResource Brush.InputBorderFocus}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="22"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="ValueTextBox"
                     Grid.Column="0"
                     Style="{StaticResource Inner.TextBox}"/>

            <!-- Colonna frecce: bordo sinistro sottile + due RepeatButton -->
            <Border Grid.Column="1"
                    BorderBrush="{DynamicResource Brush.Border}"
                    BorderThickness="1,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="1"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <RepeatButton x:Name="IncrementBtn"
                                  Grid.Row="0"
                                  Style="{StaticResource SpinBtn}"
                                  Delay="500" Interval="80">
                        <TextBlock Text="&#xE70E;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="7" FontWeight="Normal"
                                   Foreground="{DynamicResource Brush.TextSecondary}"/>
                    </RepeatButton>

                    <Rectangle Grid.Row="1"
                               Fill="{DynamicResource Brush.Border}"/>

                    <RepeatButton x:Name="DecrementBtn"
                                  Grid.Row="2"
                                  Style="{StaticResource SpinBtn}"
                                  Delay="500" Interval="80">
                        <TextBlock Text="&#xE70D;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="7" FontWeight="Normal"
                                   Foreground="{DynamicResource Brush.TextSecondary}"/>
                    </RepeatButton>

                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
