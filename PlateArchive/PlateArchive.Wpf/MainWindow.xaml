<Window x:Class="PlateArchive.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="PlateArchive" Height="680" Width="1100"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#90A4AE"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,13"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#263340"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#1E2A3B">
            <DockPanel>
                <!-- Logo / Titolo app -->
                <Border DockPanel.Dock="Top" Background="#17202D" Padding="20,18">
                    <TextBlock Text="PlateArchive"
                               Foreground="White"
                               FontSize="17"
                               FontWeight="SemiBold"/>
                </Border>

                <!-- Separatore -->
                <Rectangle DockPanel.Dock="Top" Height="1" Fill="#263340"/>

                <!-- Voci di navigazione -->
                <StackPanel DockPanel.Dock="Top" Margin="0,8">
                    <Button Content="Dashboard"
                            Command="{Binding NavigateToDashboardCommand}"
                            Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Clienti"
                            Command="{Binding NavigateToClientiCommand}"
                            Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Piastre"
                            Command="{Binding NavigateToPiastreCommand}"
                            Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Macchine"
                            Command="{Binding NavigateToMacchineCommand}"
                            Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Disegni"
                            Command="{Binding NavigateToDisegniCommand}"
                            Style="{StaticResource NavButtonStyle}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Area contenuto: il ContentControl usa i DataTemplate di App.xaml -->
        <ContentControl Grid.Column="1"
                        Content="{Binding CurrentViewModel}"
                        Background="White"/>
    </Grid>
</Window>
